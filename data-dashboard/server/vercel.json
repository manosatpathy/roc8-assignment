{
  "version": 2,
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "npx prisma generate && vercel build --force",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.js"
    }
  ]
}
